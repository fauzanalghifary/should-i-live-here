# Should I Live Here?

A small web product for quick first-pass neighborhood livability checks.

## Development

```bash
pnpm install
pnpm nx show projects
```

The workspace is intentionally skeletal for now. The `web` and `api` apps will be filled in incrementally.

